services:
  # Site
  web:
    container_name: web
    build:
      context: web
      dockerfile: Dockerfile
    restart: unless-stopped
    volumes:
      - ./web:/app
      - /app/node_modules
      - /app/.astro
    ports:
      - 3000:3000
    networks:
      - rao

networks:
  rao:
